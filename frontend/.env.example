NEXT_PUBLIC_API_URL=http://localhost:8080
NEXT_PUBLIC_ENABLE_API_MOCKING=true

# URL dobot robot service (Flask :5001) — dipakai Monitor 2 untuk liveview kamera
# deteksi tangan + status gesture/lock-unlock. Kosongkan untuk auto-derive dari
# hostname halaman + :5001 (cocok untuk PC kiosk & device LAN).
NEXT_PUBLIC_ROBOT_URL=http://localhost:5001
