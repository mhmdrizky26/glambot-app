import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Choose Your Package',
  description: 'Select the photobooth experience that suits you',
};

export { default } from '@/features/public/package/pages/PackagePage';
